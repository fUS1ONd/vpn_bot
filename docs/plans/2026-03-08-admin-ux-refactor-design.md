# Рефакторинг UX админ-панели

**Дата:** 2026-03-08
**Статус:** утверждён

## Цель

1. Перегруппировать кнопки админ-панели по логическим категориям вместо одной свалки "Управление"
2. Унифицировать отображение пользователей/модераторов — единый формат с `|` разделителем, deep link и копируемым ID

## Текущая структура кнопок

```
/start (админ)
├── 📋 Управление
│   ├── 🎟 Создать инвайт
│   ├── 📋 Коды
│   ├── 🚫 Забанить
│   ├── 🗑 Удалить код
│   ├── ♾️ Сменить тариф
│   ├── 👥 Модераторы
│   │   ├── ➕ Назначить модератора
│   │   ├── 📋 Список модераторов
│   │   ├── 📊 Статистика
│   │   ├── ➖ Снять модератора
│   │   └── 🔙 В меню админа
│   └── 🔙 В меню админа
├── 📢 Рассылка
│   ├── 📢 Рассылка активным
│   └── 🔙 В меню админа
└── 👤 Режим пользователя
```

## Новая структура кнопок

```
/start (админ)
├── 📋 Управление
│   ├── ➕ Создать инвайт
│   ├── 📋 Просмотр кодов
│   ├── 🗑 Удалить код
│   ├── 🚫 Забанить
│   ├── ♾️ Сменить тариф
│   └── 🔙 В меню админа
├── 🛡 Модераторы
│   ├── ➕ Назначить
│   ├── 📋 Список
│   ├── 📊 Статистика
│   ├── ➖ Снять
│   └── 🔙 В меню админа
├── 📢 Рассылка
│   ├── 📢 Рассылка активным
│   └── 🔙 В меню админа
└── 👤 Режим пользователя
```

Главное изменение: модераторы вынесены на верхний уровень, "Управление" содержит инвайты + действия над пользователями (связанные сущности — смотришь код, видишь ID, действуешь).

## Унификация отображения пользователей

### Единый формат

```
👤 Имя | @username | 123456789
```

- **Имя** — всегда deep link `tg://user?id=...`. Если имени нет — "Пользователь"
- **@username** — если есть. Если нет — пропускается
- **123456789** — всегда `<code>` (моно, копируемый по тапу)
- **Разделитель** — ` | ` вместо ` • `

### Варианты отображения

С username и именем:
```
👤 Иван | @ivan | 123456789
```

С именем, без username:
```
👤 Иван | 123456789
```

Без имени, без username:
```
👤 Пользователь | 123456789
```

### Места замены (5 штук)

1. **Просмотр кодов — админ** (`admin.go`, `formatInviteEntry`)
   - Было: `👤 @ivan (123456789) • Иван`
   - Стало: `👤 Иван | @ivan | 123456789`

2. **Список модераторов — админ** (`admin.go`, `handleAdminListModerators`)
   - Было: `👤 @moderator • Иван` + `🆔 123456789`
   - Стало: `👤 Иван | @moderator | 123456789` (одна строка вместо двух)

3. **Статистика модераторов — админ** (`admin.go`, `handleAdminModStats`)
   - Было: `👤 @moderator • Иван`
   - Стало: `👤 Иван | @moderator | 123456789`

4. **Просмотр инвайтов — модератор** (`moderator.go`, `handleModViewInvites`)
   - Было: `👤 @ivan • Иван • ID: 123456789`
   - Стало: `👤 Иван | @ivan | 123456789`

5. **Список подписчиков — модератор** (`moderator.go`, `handleModSubscribers` + `formatSubscriberLabel`)
   - Было: `✅ @ivan • Иван • ID: 123456789`
   - Стало: `✅ Иван | @ivan | 123456789`

## Технические изменения

### `internal/bot/keyboards.go`

- Убрать `AdminManageKeyboard()` (свалка с модераторами)
- Новая `AdminManageKeyboard()` — без кнопки "Модераторы" (инвайты + пользователи)
- `AdminKeyboard()` — 4 кнопки: Управление, Модераторы, Рассылка, Режим пользователя
- `AdminModeratorKeyboard()` — без изменений
- `AdminBroadcastKeyboard()` — без изменений

### `internal/bot/handlers.go`

- Кнопка `BtnAdminModerators` обрабатывается на верхнем уровне (а не внутри Управления)

### `internal/bot/admin.go`

- Обновить `formatInviteEntry` — новый формат пользователя
- Обновить `handleAdminListModerators` — новый формат, убрать отдельную строку `🆔`
- Обновить `handleAdminModStats` — новый формат

### `internal/bot/moderator.go`

- Обновить `handleModViewInvites` — новый формат
- Обновить `handleModSubscribers` — новый формат
- Обновить `formatSubscriberLabel` — разделитель ` | ` вместо ` • `

### Общая утилита (опционально)

Можно вынести `formatUserLabel(name, username string, telegramID int64) string` как общую функцию, чтобы не дублировать логику формирования строки в 5 местах.

### Тесты

- Обновить ожидания в тестах, которые проверяют текст вывода с `•`

## Найденные баги после реализации (code review)

### Medium: дублирование ID в списке подписчиков модератора

`handleModSubscribers` (`moderator.go:174, 184`) дописывает ` • ID: <code>%d</code>` поверх результата
`formatSubscriberLabel`, который уже содержит ID. Итог: ID показывается дважды, разделитель `•` остался.

Фикс: убрать ` • ID: <code>%d</code>` из строк 174 и 184, оставить только `%s`.

### Medium: дублирование ID в диалоге продления подписки

`handleModExtend` (`moderator.go:219`) строит строку `• <code>%d</code> — %s`, где `%s` =
`formatSubscriberLabel` с ID внутри. Итог: ID дублируется, `•` смешан со старым форматом.

Фикс: заменить на `• %s` без отдельного ID.

### Medium: firstName не экранируется в HTML

`formatUserLabel` (`format.go:13`) вставляет `firstName` в HTML-атрибут и тело тега без экранирования.
Имя вроде `<b>Alex</b>` или `Tom & Jerry` ломает разметку во всех экранах на ModeHTML.

Фикс: экранировать `firstName` через `html.EscapeString`.

### Low: success-сообщение смены тарифа без ParseMode HTML

`processSwitchSubscriptionConfirm` (`admin.go:235`) отправляет сообщение без `ParseMode: tele.ModeHTML`,
хотя `session.TargetLabel` в fallback-сценарии (нет имени, нет username) содержит `<code>123</code>`.
Пользователь видит literal-теги.

Фикс: добавить `ParseMode: tele.ModeHTML` в SendOptions.

### Low: тест закрепил старый формат вместо нового

`moderator_test.go:264` проверяет `"ID: <code>300</code>"` — старый формат, которого больше нет.
Тест проходит только потому, что `handleModSubscribers` ещё дописывает ID вручную (баг #1).
После фикса #1 тест должен ожидать новый формат через `formatUserLabel`.
