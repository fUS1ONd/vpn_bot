# VPN Bot (Remnawave)

Telegram-бот управления VPN на базе [Remnawave](https://remnawave.com).
Пользователи регистрируются по инвайт-кодам, управляют подключениями и отслеживают трафик.
Поддерживает пользовательский и административный режимы.

## Быстрый старт

```bash
git clone https://github.com/<user>/vpn_bot.git
cd vpn_bot
cp .env.example .env
# Заполни .env своими ключами
make up
```

Логи:

```bash
make logs
```

## Конфигурация

| Переменная                     | Обязательная | Описание                                                    |
| ------------------------------ | ------------ | ----------------------------------------------------------- |
| `BOT_TOKEN`                    | ✅           | Токен от @BotFather                                         |
| `ADMIN_ID`                     | ✅           | Telegram ID администратора (получи у @userinfobot)          |
| `REMNAWAVE_URL`                | ✅           | URL панели Remnawave                                        |
| `REMNAWAVE_API_TOKEN`          | ✅           | JWT-токен из панели Remnawave                               |
| `DB_PATH`                      | —            | Путь к SQLite-базе (дефолт: `/app/data/bot.db`)             |
| `REMNAWAVE_DEFAULT_SQUAD_UUIDS` | —            | Список UUID internal squads через запятую; новые пользователи добавляются во все перечисленные сквады |
| `SD_CONFIGS_PATH`              | —            | Путь к папке service discovery для мониторинга (дефолт: `/app/sd_configs`) |
| `VICTORIA_METRICS_URL`         | —            | URL VictoriaMetrics (дефолт: `http://victoriametrics:8428`) |
| `CALLBACK_PORT`                | —            | Порт встроенного callback-сервера Platega (дефолт: `8080`)  |
| `MIN_SUBSCRIPTION_PRICE`       | —            | Минимальная цена ручного изменения тарифа; fallback для старой конфигурации |
| `DEFAULT_SUBSCRIPTION_PRICE`   | —            | Единая цена новых referral-приглашений, фиксируется при создании (fallback: `MIN_SUBSCRIPTION_PRICE`, затем `400`) |
| `ADMIN_TEST_PAYMENT_PRICE`     | —            | Тестовая цена для оплаты от `ADMIN_ID`; `0` или отсутствие выключает кнопку |
| `TRIAL_TRAFFIC_LIMIT_GB`       | —            | Лимит трафика для триала в ГБ (дефолт: `1`)                 |
| `PLATEGA_MERCHANT_ID`          | —            | Merchant ID Platega для пользовательской оплаты             |
| `PLATEGA_SECRET`               | —            | Секретный ключ Platega                                      |
| `PLATEGA_CALLBACK_URL`         | —            | Публичный HTTPS callback URL вида `https://domain/platega/callback` |
| `PLATEGA_FEE_SBP`              | —            | Комиссия Platega для СБП в процентах                        |
| `PLATEGA_FEE_CARD`             | —            | Комиссия Platega для оплаты картой                          |
| `PLATEGA_FEE_CRYPTO`           | —            | Комиссия Platega для крипты                                 |
| `PLATEGA_FEE_WITHDRAWAL`       | —            | Архивная настройка комиссии вывода для старых финансовых записей |
| `YOOKASSA_SHOP_ID`             | —            | Shop ID ЮKassa; включается только вместе с секретным ключом |
| `YOOKASSA_SECRET_KEY`          | —            | Секретный ключ ЮKassa                                       |
| `YOOKASSA_RETURN_URL`          | —            | URL возврата в Telegram после оплаты                        |
| `YOOKASSA_FEE_PERCENT`         | —            | Фактическая договорная комиссия ЮKassa в %, допускается дробная (например, `3.5`) |
| `RENDER_URL`                   | —            | URL render-сервиса субтитров                                |
| `RENDER_API_KEY`               | —            | API-ключ render-сервиса                                     |

Пример:

```env
REMNAWAVE_DEFAULT_SQUAD_UUIDS=uuid-1,uuid-2,uuid-3
```

Для обратной совместимости бот также понимает legacy-переменную `REMNAWAVE_DEFAULT_SQUAD_UUID`, если новый список не задан.

Platega и ЮKassa независимы: можно включить любой один провайдер или оба. При обоих включённых ЮKassa показывает карту/СБП/SberPay, а Platega — криптовалюту. Callback-сервер поднимается, если настроен хотя бы один провайдер.
Перед production-включением ЮKassa владелец магазина обязан подтвердить фискализацию и выдачу чеков в своём юридическом сценарии (чеки ЮKassa, внешняя касса или отдельный процесс). Эта интеграция не создаёт `receipt` без подтверждённых реквизитов.
Подробности по интеграции Platega и настройке callback-сервера: [docs/platega/README.md](docs/platega/README.md) и [docs/plans/2026-03-22-deployment-checklist.md](docs/plans/2026-03-22-deployment-checklist.md).

## Функциональность

### Пользователь

| Кнопка          | Действие                                                             |
| --------------- | -------------------------------------------------------------------- |
| `/start`        | Регистрация по инвайт-коду или вход для зарегистрированных           |
| `/start <code>` | Автоматическая активация кода из ссылки-приглашения                  |
| `👤 Моя подписка` | Карточка подписки одним сообщением: статус (триал / оплаченная / grace / бессрочная), трафик, устройства и ссылка для ручного подключения (тап по ней копирует текст). Ниже inline-кнопки: `🔗 Подключить устройство`, `📱 Мои устройства`, `🔄 Перевыпустить ссылку` |
| `🔗 Подключить устройство` | Открывает страницу подписки Remnawave: приложения под платформу и подключение в один тап. Инструкции живут там и настраиваются в панели |
| `📱 Мои устройства` | Список подключённых HWID-устройств; позволяет удалить одно устройство или сбросить все. `🔙 Назад` возвращает карточку подписки |
| `🔄 Перевыпустить ссылку` | Выпускает новую ссылку подписки: старая перестаёт работать сразу, все устройства сбрасываются. Требует подтверждения, кулдаун 30 секунд |
| `💳 Оплатить подписку` / `💳 Продлить подписку` | Запуск flow оплаты: выбор способа, ссылка, ручная проверка; кнопка скрыта без Platega или при `subscription_price = NULL` |
| `📡 Серверы`    | Live-дашборд мониторинга нод (обновляется каждые 5 сек)              |
| `ℹ️ Информация` | Помощь, контакт для вопросов и ссылки на документы сервиса           |
| `🛠 Сообщить о проблеме` | Мультивыбор включённых серверов (с галочками ✅) или «🤷 Не знаю / все сразу» → выбор категории (Не подключается / Медленно работает / Не грузит сайт/сервис / Другое) → опциональный комментарий → отправка структурированного репорта админу. Кулдаун 10 минут на пользователя |
| `🎟 Приглашения` | Создание, повторная отправка и отзыв своих приглашений; раздел виден всем зарегистрированным пользователям |

### Подключение и ссылка подписки

Подключение целиком опирается на страницу подписки Remnawave: отдельного раздела инструкций в боте нет,
приложения и шаги настройки редактируются в панели и обновляются без релиза бота.

- Сразу после регистрации бот присылает два сообщения: приветствие с меню и отдельное сообщение с кнопкой
  подключения и ссылкой (Telegram не допускает reply- и inline-разметку в одном сообщении).
- Кнопки `🔗 Подключить устройство` и `🔄 Перевыпустить ссылку` показываются там же, где показывается сама
  ссылка: у активной и бессрочной подписки. В grace period и при исчерпанном трафике триала их нет.
- `📱 Мои устройства` доступны любому зарегистрированному пользователю.
- Перевыпуск сначала сбрасывает все HWID-устройства, затем меняет ссылку в панели. Если сброс не удался,
  ссылка остаётся прежней. Кнопки в старых сообщениях «Моя подписка» после перевыпуска ведут на нерабочую
  ссылку — раздел нужно открыть заново.

### Приглашения пользователей

Создавать новые ссылки могут пользователи с действующей оплаченной, перенесённой legacy-paid или бессрочной подпиской. Пробный пользователь и пользователь в grace period могут смотреть старые ссылки, но не создавать новые.

- Ссылка действует 30 суток, даёт приглашённому 72 часа trial и `TRIAL_TRAFFIC_LIMIT_GB` трафика.
- Цена берётся из `DEFAULT_SUBSCRIPTION_PRICE` и сохраняется в ссылке: будущая смена настройки её не изменяет.
- Одновременно разрешено 3 активных ссылки и не более 15 созданий за скользящие 24 часа.
- Активную ссылку можно в любой момент повторно отправить или мягко отозвать. История использованных ссылок сохраняется.
- После trial без оплаты доступ отключается, затем пользователь автоматически исключается существующим scheduler-flow.
- Первый пригласивший сохраняется навсегда для статистики, даже если пользователь позже вернулся по другой ссылке.
- Повторный trial после автокика пока сознательно разрешён; lifetime anti-abuse не входит в эту версию.
- Выплаты и новые начисления авторам приглашений отсутствуют.

### Администратор

Главное меню администратора:

| Кнопка                  | Действие                                                                       |
| ----------------------- | ------------------------------------------------------------------------------ |
| `📋 Управление`         | Инвайты и действия с пользователями (см. ниже)                                 |
| `🤝 Приглашения`       | Обзор созданий и активаций, конверсия, рейтинг приглашающих                    |
| `📢 Рассылка`           | Отправка сообщения всем активным пользователям (текст, фото, видео, документы) |
| `📊 Общая статистика`   | Финансовая сводка за месяц, конверсия trial → оплата и текущее состояние базы   |
| `🔧 Режим обслуживания` / `▶️ Штатный режим` | Временно скрывает оплату у пользователей и останавливает scheduler-кики/disable |
| `👤 Режим пользователя` | Просмотр бота глазами пользователя                                             |

Кнопка `📋 Управление` открывает панель инвайтов и действий с пользователями:

| Кнопка              | Действие                                                                           |
| ------------------- | ---------------------------------------------------------------------------------- |
| `🎟 Создать инвайт` | Генерирует инвайт-код и ссылку для отправки                                        |
| `🚫 Забанить`       | Перманентный бан: запись в ban-лист + удаление из БД бота и Remnawave             |
| `♾️ Сменить тариф`  | Перевод пользователя с месячной подписки на бессрочную                             |
| `🔍 Инфо о пользователе` | Карточка пользователя: первый пригласивший, цена, срок, трафик, устройства, статус и кнопки продления/приглашений; админ может отозвать чужую активную ссылку |
| `✏️ Изменить цену`  | Смена цены месячной подписки; для legacy-пользователя без цены бот отдельно спросит, считать ли текущий период уже оплаченным |

При активации нового инвайта админ получает уведомление с Telegram ID, username и именем.

## Поведение в граничных случаях

**Два вида инвайтов**:
- referral-инвайт пользователя (включая администратора в пользовательском режиме): 72-часовой trial, глобальная snapshot-цена, срок жизни ссылки 30 суток и учёт в рейтинге;
- служебный админский инвайт из `📋 Управление`: бессрочно (`2099-01-01`), без цены, трафик-лимита и referral-статистики.

**Срок подписки и продление**:
- продление через Platega добавляет 30 дней к текущему сроку (или от текущей даты, если уже истекло);
- раннее продление через Platega запрещено, если до истечения больше 90 дней;
- при сбое активации платёж остаётся подтверждённым, получает статус `confirmed_not_activated` и повторно обрабатывается scheduler без перезаписи исходного `confirmed_at`;
- администратор может перевести месячную подписку в бессрочную; при этом `invites.created_by` сохраняется, а `expire_days` становится `NULL`.

**Scheduler подписок**:
1. стартует с полным проходом при запуске бота и далее работает каждые 30 минут;
2. триал предупреждает менее чем за 24 часа и кикает сразу в точный `expireAt`;
3. оплаченную подписку предупреждает за 3 дня и менее чем за 24 часа;
4. в точный `expireAt` оплаченная подписка переводится в `DISABLED`, затем даётся 72 часа grace period;
5. после grace period пользователь удаляется, если свежая оплата не подтверждена;
6. в `maintenance mode` disable и автокики блокируются.
7. legacy-пользователи без инвайта и без `subscription_price` пропускаются новым payment-scheduler и продолжают жить по старой модели.
8. legacy-пользователь с уже оплаченным вручную текущим периодом может быть переведён на новую модель через `✏️ Изменить цену`:
   бот спросит, считать ли текущий период уже оплаченным; при ответе `Да` пользователь сразу пойдёт по paid-ветке с `💳 Продлить подписку`, напоминаниями за 3/1 день и grace period.

**Flow оплаты**:
1. пользователь выбирает `💳 Оплатить подписку` или `💳 Продлить подписку`;
2. бот показывает методы оплаты: `🏦 СБП`, `💳 Карта`, `🪙 Крипта`;
3. после создания платежа бот отправляет ссылку и кнопку `🔄 Проверить оплату`;
4. callback и ручная проверка синхронизируют локальный статус платежа (`confirmed`, `canceled`, `chargebacked`);
5. при успешной оплате подписка активируется автоматически, лимит трафика снимается;
6. если подтверждение получено, но Remnawave временно недоступен, пользователю не показывается ложное сообщение об активации: платёж остаётся в `confirmed_not_activated`, а scheduler повторяет активацию позже.

**Миграция старой moderator-модели**:
1. старые использованные moderator-инвайты становятся referral-историей, а `users.invited_by` заполняется первым автором;
2. старые неиспользованные referral-ссылки мягко отзываются, служебные админские ссылки остаются активными;
3. таблицы и поля модераторов/earnings сохраняются только как архив, новые начисления не создаются;
4. если у старого пользователя ещё нет `subscription_price`, админ задаёт цену через `✏️ Изменить цену`;
5. для legacy-case бот дополнительно спрашивает, был ли текущий период уже оплачен вручную;
6. ответ `✅ Да, считать оплаченной` помечает пользователя как migrated-paid без поддельного платежа в `payments`; ответ `❌ Нет, оставить trial` оставляет trial до первой оплаты;
7. вопрос показывается только для legacy-case с пустой ценой, активной finite-подпиской и отсутствием confirmed-платежей.

**Бан пользователя** — перманентная операция:

1. Telegram ID заносится в `banned_users`
2. Пользователь удаляется из Remnawave
3. Пользователь удаляется из БД бота
После бана пользователь блокируется на уровне `/start` и не может зарегистрироваться повторно.

**Автокик по истечению подписки** — это не бан. Пользователь может вернуться с новым инвайтом.

**Рассылка** — отправляется только пользователям со статусом `ACTIVE` в Remnawave. Пользователи с истёкшей подпиской или заблокированные не получают.

## Мониторинг нод

Бот мониторит VPS-ноды через Prometheus Node Exporter и отображает их состояние в live-дашборде (кнопка `📡 Серверы`). При уходе ноды в офлайн или перегрузке приходит алерт в Telegram.

### Настройка

На каждой ноде выполни:

```bash
bash <(wget -qO- https://raw.githubusercontent.com/fUS1ONd/vpn_bot/main/scripts/install-node-exporter.sh) <IP_СЕРВЕРА_С_БОТОМ>
```

Скрипт устанавливает Node Exporter, открывает порт 9100 только для IP сервера с ботом и добавляет сервис в автозапуск.

В Remnawave добавь тег `bw:<Mbps>` к ноде для указания bandwidth (например `bw:1000` = 1 Gbit). По умолчанию 1000 Mbps.

### Алерты

- Нода **OFFLINE** — Node Exporter не отвечает
- **Load Index > 80%** — нода перегружена (`Load Average × 100 / bandwidth`)

## Разработка

```bash
go mod download     # Установить зависимости
make down           # Остановить бота
make up             # Пересобрать и запустить
make tests          # Запустить тесты
make fmt            # Проверить форматирование
make logs           # Показать логи
```

**Структура:**

```
vpn_bot/
├── cmd/
│   ├── bot/               → Точка входа бота
│   └── migrator/          → Утилита миграции пользователей из старой БД
├── internal/
│   ├── bot/               → Обработчики, клавиатуры, дашборд, состояния, scheduler
│   ├── callback/          → HTTP callback-сервер Platega
│   ├── database/          → SQLite (users, invites, payments, bans, notifications; moderator-таблицы сохранены как архив)
│   ├── platega/           → HTTP-клиент Platega
│   ├── remnawave/         → HTTP-клиент Remnawave API
│   ├── monitoring/        → Метрики, алерты, service discovery
│   └── config/            → Загрузка конфигурации
├── scripts/
│   └── install-node-exporter.sh  → Установка Node Exporter на ноды
├── monitoring/
│   └── prometheus.yml     → Конфиг vmagent
├── docs/                  → API документация, планы, прогресс
├── Makefile
└── docker-compose.yml     → bot + VictoriaMetrics + vmagent
```

## Ссылки

- [Remnawave](https://remnawave.com)
- [Документация Platega](docs/platega/README.md)
- [Чеклист деплоя платежей](docs/plans/2026-03-22-deployment-checklist.md)
- [CLAUDE.md](CLAUDE.md) / [AGENTS.md](AGENTS.md) — инструкции для работы с репозиторием
